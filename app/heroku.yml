name: CI/CD Pipeline
on:
  push:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'
    - name: Install dependencies
      run: pip install -r app/requirements.txt
    - name: Test Flask app
      run: python -m unittest discover
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    - name: Build and push Docker image to Docker Hub
      run: |
        docker build -t thorlongus/wine-quality-pred:latest .
        docker push thorlongus/wine-quality-pred:latest
    - name: Login to Heroku Container Registry
      run: echo "${{ secrets.HEROKU_API_KEY }}" | docker login --username=_ --password-stdin registry.heroku.com
    - name: Build and push to Heroku
      run: |
        docker build -t registry.heroku.com/wine-quality-pred/web .
        docker push registry.heroku.com/wine-quality-pred/web
    - name: Release to Heroku
      run: heroku container:release web --app wine-quality-pred
      env:
        HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}